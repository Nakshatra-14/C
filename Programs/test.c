#include <stdio.h>
int main()
{
    char c = "A";
    // printf("Enter a character: ");
    // scanf("%d", &c);                // Read a character and store it in the variable c.
    printf("%c\n", c); // Print the character.
    return 0;
}